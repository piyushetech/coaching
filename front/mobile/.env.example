# Same Node backend as the courses web app (port 4000, MongoDB sankalp DB).
# Copy to .env and adjust for physical devices (use your PC's LAN IP).

# Android emulator:  http://10.0.2.2:4000
# iOS simulator:     http://localhost:4000
# Physical phone:    http://192.168.x.x:4000
EXPO_PUBLIC_API_URL=http://localhost:4000
